import Services from "@/components/Services";

export const metadata = {
  title: "Services | ZyncPoint",
};

export default function ServicesPage() {
  return (
    <main className="min-h-screen">
      <Services />
    </main>
  );
}
