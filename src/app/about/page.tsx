import About from "@/components/About";

export const metadata = {
  title: "About Us | ZyncPoint",
};

export default function AboutPage() {
  return (
    <main className="min-h-screen">
      <About />
    </main>
  );
}
